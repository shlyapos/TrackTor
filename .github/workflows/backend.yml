image: microsoft/dotnet:latest

stages:
    - build
    - test

before_script:
    - "cd Backend/TrackTor"
    - "dotnet restore"

build:
    stage: build
    script:
        - "dotnet build"

test:
    stage: test
    script: 
        - "cd Backend/TrackTor/TrackTor.Tests"
        - "dotnet test"